// Copyright © 2023 Cisco Systems, Inc. and its affiliates.
// All rights reserved.
//
// Licensed under the Mozilla Public License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://mozilla.org/MPL/2.0/
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// SPDX-License-Identifier: MPL-2.0

// Code generated by "gen/generator.go"; DO NOT EDIT.

package provider

// Section below is generated&owned by "gen/generator.go". //template:begin imports

import (
	"context"
	"fmt"
	"strconv"

	"github.com/CiscoDevNet/terraform-provider-nxos/internal/provider/helpers"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/netascode/go-nxos"
	"github.com/tidwall/gjson"
	"github.com/tidwall/sjson"
)

// End of section. //template:end imports

// Section below is generated&owned by "gen/generator.go". //template:begin types

type Netflow struct {
	Device           types.String                       `tfsdk:"device"`
	Dn               types.String                       `tfsdk:"id"`
	Exporters        map[string]NetflowExporters        `tfsdk:"exporters"`
	Records          map[string]NetflowRecords          `tfsdk:"records"`
	Monitors         map[string]NetflowMonitors         `tfsdk:"monitors"`
	HardwareProfiles map[string]NetflowHardwareProfiles `tfsdk:"hardware_profiles"`
	ClassMaps        map[string]NetflowClassMaps        `tfsdk:"class_maps"`
}

type NetflowExporters struct {
	Description     types.String `tfsdk:"description"`
	DestinationIp   types.String `tfsdk:"destination_ip"`
	DestinationPort types.Int64  `tfsdk:"destination_port"`
	Dscp            types.Int64  `tfsdk:"dscp"`
	SourceInterface types.String `tfsdk:"source_interface"`
	Version         types.String `tfsdk:"version"`
	VrfName         types.String `tfsdk:"vrf_name"`
}

type NetflowRecords struct {
	Description       types.String `tfsdk:"description"`
	CollectParameters types.String `tfsdk:"collect_parameters"`
	MatchParameters   types.String `tfsdk:"match_parameters"`
}

type NetflowMonitors struct {
	Description     types.String                              `tfsdk:"description"`
	ExporterBuckets map[string]NetflowMonitorsExporterBuckets `tfsdk:"exporter_buckets"`
}

type NetflowMonitorsExporterBuckets struct {
	Description types.String `tfsdk:"description"`
	HashHigh    types.Int64  `tfsdk:"hash_high"`
	HashLow     types.Int64  `tfsdk:"hash_low"`
}

type NetflowHardwareProfiles struct {
	Description        types.String `tfsdk:"description"`
	BurstIntervalShift types.Int64  `tfsdk:"burst_interval_shift"`
	ExportInterval     types.Int64  `tfsdk:"export_interval"`
	IpPacketIdShift    types.Int64  `tfsdk:"ip_packet_id_shift"`
	Mtu                types.Int64  `tfsdk:"mtu"`
	SourcePort         types.Int64  `tfsdk:"source_port"`
}

type NetflowClassMaps struct {
	MatchAcls map[string]NetflowClassMapsMatchAcls `tfsdk:"match_acls"`
}

type NetflowClassMapsMatchAcls struct {
}

type NetflowIdentity struct {
	Device types.String `tfsdk:"device"`
}

func (data *NetflowIdentity) toIdentity(ctx context.Context, plan *Netflow) {
	if plan.Device.IsNull() {
		data.Device = types.StringValue("")
	} else {
		data.Device = plan.Device
	}
}

func (data *Netflow) fromIdentity(ctx context.Context, identity *NetflowIdentity) {
	if identity.Device.ValueString() == "" {
		data.Device = types.StringNull()
	} else {
		data.Device = identity.Device
	}
}

// End of section. //template:end types

// Section below is generated&owned by "gen/generator.go". //template:begin getPath

func (data Netflow) getDn() string {
	return "sys/flow"
}

func (data NetflowExporters) getRn(key string) string {
	return fmt.Sprintf("fe-[%s]", key)
}

func (data NetflowRecords) getRn(key string) string {
	return fmt.Sprintf("fr-[%s]", key)
}

func (data NetflowMonitors) getRn(key string) string {
	return fmt.Sprintf("fm-[%s]", key)
}

func (data NetflowMonitorsExporterBuckets) getRn(key string) string {
	return fmt.Sprintf("feb-[%v]", helpers.Must(strconv.ParseInt(key, 10, 64)))
}

func (data NetflowHardwareProfiles) getRn(key string) string {
	return fmt.Sprintf("hp-[%s]", key)
}

func (data NetflowClassMaps) getRn(key string) string {
	return fmt.Sprintf("cmap-[%s]", key)
}

func (data NetflowClassMapsMatchAcls) getRn(key string) string {
	return fmt.Sprintf("acl-[%s]", key)
}

func (data Netflow) getClassName() string {
	return "flowEntity"
}

// End of section. //template:end getPath

// Section below is generated&owned by "gen/generator.go". //template:begin toBody

func (data Netflow) toBody(config Netflow) nxos.Body {
	body := ""
	body, _ = sjson.Set(body, data.getClassName()+".attributes", map[string]interface{}{})
	var attrs string
	childrenPath := data.getClassName() + ".children"
	for key, child := range data.Exporters {
		configChild, configChildOk := config.Exporters[key]
		_ = configChild
		_ = configChildOk
		attrs = "{}"
		attrs, _ = sjson.Set(attrs, "name", key)
		if configChildOk && !child.Description.IsUnknown() && !child.Description.IsNull() && !configChild.Description.IsNull() {
			attrs, _ = sjson.Set(attrs, "description", child.Description.ValueString())
		}
		if configChildOk && !child.DestinationIp.IsUnknown() && !child.DestinationIp.IsNull() && !configChild.DestinationIp.IsNull() {
			attrs, _ = sjson.Set(attrs, "destinationIp", child.DestinationIp.ValueString())
		}
		if configChildOk && !child.DestinationPort.IsUnknown() && !child.DestinationPort.IsNull() && !configChild.DestinationPort.IsNull() {
			attrs, _ = sjson.Set(attrs, "destinationPort", strconv.FormatInt(child.DestinationPort.ValueInt64(), 10))
		}
		if configChildOk && !child.Dscp.IsUnknown() && !child.Dscp.IsNull() && !configChild.Dscp.IsNull() {
			attrs, _ = sjson.Set(attrs, "dscp", strconv.FormatInt(child.Dscp.ValueInt64(), 10))
		}
		if configChildOk && !child.SourceInterface.IsUnknown() && !child.SourceInterface.IsNull() && !configChild.SourceInterface.IsNull() {
			attrs, _ = sjson.Set(attrs, "sourceInterface", child.SourceInterface.ValueString())
		}
		if configChildOk && !child.Version.IsUnknown() && !child.Version.IsNull() && !configChild.Version.IsNull() {
			attrs, _ = sjson.Set(attrs, "version", child.Version.ValueString())
		}
		if configChildOk && !child.VrfName.IsUnknown() && !child.VrfName.IsNull() && !configChild.VrfName.IsNull() {
			attrs, _ = sjson.Set(attrs, "vrfName", child.VrfName.ValueString())
		}
		body, _ = sjson.SetRaw(body, childrenPath+".-1.flowExporter.attributes", attrs)
	}
	for key, child := range data.Records {
		configChild, configChildOk := config.Records[key]
		_ = configChild
		_ = configChildOk
		attrs = "{}"
		attrs, _ = sjson.Set(attrs, "name", key)
		if configChildOk && !child.Description.IsUnknown() && !child.Description.IsNull() && !configChild.Description.IsNull() {
			attrs, _ = sjson.Set(attrs, "description", child.Description.ValueString())
		}
		if configChildOk && !child.CollectParameters.IsUnknown() && !child.CollectParameters.IsNull() && !configChild.CollectParameters.IsNull() {
			attrs, _ = sjson.Set(attrs, "collectParams", child.CollectParameters.ValueString())
		}
		if configChildOk && !child.MatchParameters.IsUnknown() && !child.MatchParameters.IsNull() && !configChild.MatchParameters.IsNull() {
			attrs, _ = sjson.Set(attrs, "matchParams", child.MatchParameters.ValueString())
		}
		body, _ = sjson.SetRaw(body, childrenPath+".-1.flowRecord.attributes", attrs)
	}
	for key, child := range data.Monitors {
		configChild, configChildOk := config.Monitors[key]
		_ = configChild
		_ = configChildOk
		attrs = "{}"
		attrs, _ = sjson.Set(attrs, "name", key)
		if configChildOk && !child.Description.IsUnknown() && !child.Description.IsNull() && !configChild.Description.IsNull() {
			attrs, _ = sjson.Set(attrs, "description", child.Description.ValueString())
		}
		body, _ = sjson.SetRaw(body, childrenPath+".-1.flowMonitor.attributes", attrs)
		{
			nestedIndex := len(gjson.Get(body, childrenPath).Array()) - 1
			nestedChildrenPath := childrenPath + "." + strconv.Itoa(nestedIndex) + ".flowMonitor.children"
			_ = nestedChildrenPath
			for key, child := range child.ExporterBuckets {
				configChild, configChildOk := configChild.ExporterBuckets[key]
				_ = configChild
				_ = configChildOk
				attrs = "{}"
				attrs, _ = sjson.Set(attrs, "id", key)
				if configChildOk && !child.Description.IsUnknown() && !child.Description.IsNull() && !configChild.Description.IsNull() {
					attrs, _ = sjson.Set(attrs, "description", child.Description.ValueString())
				}
				if configChildOk && !child.HashHigh.IsUnknown() && !child.HashHigh.IsNull() && !configChild.HashHigh.IsNull() {
					attrs, _ = sjson.Set(attrs, "hashHi", strconv.FormatInt(child.HashHigh.ValueInt64(), 10))
				}
				if configChildOk && !child.HashLow.IsUnknown() && !child.HashLow.IsNull() && !configChild.HashLow.IsNull() {
					attrs, _ = sjson.Set(attrs, "hashLo", strconv.FormatInt(child.HashLow.ValueInt64(), 10))
				}
				body, _ = sjson.SetRaw(body, nestedChildrenPath+".-1.flowExporterBucket.attributes", attrs)
			}
		}
	}
	for key, child := range data.HardwareProfiles {
		configChild, configChildOk := config.HardwareProfiles[key]
		_ = configChild
		_ = configChildOk
		attrs = "{}"
		attrs, _ = sjson.Set(attrs, "name", key)
		if configChildOk && !child.Description.IsUnknown() && !child.Description.IsNull() && !configChild.Description.IsNull() {
			attrs, _ = sjson.Set(attrs, "description", child.Description.ValueString())
		}
		if configChildOk && !child.BurstIntervalShift.IsUnknown() && !child.BurstIntervalShift.IsNull() && !configChild.BurstIntervalShift.IsNull() {
			attrs, _ = sjson.Set(attrs, "burstIntervalShift", strconv.FormatInt(child.BurstIntervalShift.ValueInt64(), 10))
		}
		if configChildOk && !child.ExportInterval.IsUnknown() && !child.ExportInterval.IsNull() && !configChild.ExportInterval.IsNull() {
			attrs, _ = sjson.Set(attrs, "exportInterval", strconv.FormatInt(child.ExportInterval.ValueInt64(), 10))
		}
		if configChildOk && !child.IpPacketIdShift.IsUnknown() && !child.IpPacketIdShift.IsNull() && !configChild.IpPacketIdShift.IsNull() {
			attrs, _ = sjson.Set(attrs, "ipPacketIdShift", strconv.FormatInt(child.IpPacketIdShift.ValueInt64(), 10))
		}
		if configChildOk && !child.Mtu.IsUnknown() && !child.Mtu.IsNull() && !configChild.Mtu.IsNull() {
			attrs, _ = sjson.Set(attrs, "mtu", strconv.FormatInt(child.Mtu.ValueInt64(), 10))
		}
		if configChildOk && !child.SourcePort.IsUnknown() && !child.SourcePort.IsNull() && !configChild.SourcePort.IsNull() {
			attrs, _ = sjson.Set(attrs, "sourcePort", strconv.FormatInt(child.SourcePort.ValueInt64(), 10))
		}
		body, _ = sjson.SetRaw(body, childrenPath+".-1.flowHwProfile.attributes", attrs)
	}
	for key, child := range data.ClassMaps {
		configChild, configChildOk := config.ClassMaps[key]
		_ = configChild
		_ = configChildOk
		attrs = "{}"
		attrs, _ = sjson.Set(attrs, "name", key)
		body, _ = sjson.SetRaw(body, childrenPath+".-1.flowClassMap.attributes", attrs)
		{
			nestedIndex := len(gjson.Get(body, childrenPath).Array()) - 1
			nestedChildrenPath := childrenPath + "." + strconv.Itoa(nestedIndex) + ".flowClassMap.children"
			_ = nestedChildrenPath
			for key := range child.MatchAcls {
				configChild, configChildOk := configChild.MatchAcls[key]
				_ = configChild
				_ = configChildOk
				attrs = "{}"
				attrs, _ = sjson.Set(attrs, "name", key)
				body, _ = sjson.SetRaw(body, nestedChildrenPath+".-1.flowMatchAcl.attributes", attrs)
			}
		}
	}

	return nxos.Body{Str: body}
}

// End of section. //template:end toBody

// Section below is generated&owned by "gen/generator.go". //template:begin fromBody

func (data *Netflow) fromBody(res gjson.Result) {
	res.Get(data.getClassName() + ".children").ForEach(
		func(_, v gjson.Result) bool {
			v.ForEach(
				func(classname, value gjson.Result) bool {
					if classname.String() == "flowExporter" {
						var child NetflowExporters
						child.Description = types.StringValue(value.Get("attributes.description").String())
						child.DestinationIp = types.StringValue(value.Get("attributes.destinationIp").String())
						child.DestinationPort = types.Int64Value(value.Get("attributes.destinationPort").Int())
						child.Dscp = types.Int64Value(value.Get("attributes.dscp").Int())
						child.SourceInterface = types.StringValue(value.Get("attributes.sourceInterface").String())
						child.Version = types.StringValue(value.Get("attributes.version").String())
						child.VrfName = types.StringValue(value.Get("attributes.vrfName").String())
						mapKey := value.Get("attributes.name").String()
						if data.Exporters == nil {
							data.Exporters = make(map[string]NetflowExporters)
						}
						data.Exporters[mapKey] = child
					}
					return true
				},
			)
			return true
		},
	)
	res.Get(data.getClassName() + ".children").ForEach(
		func(_, v gjson.Result) bool {
			v.ForEach(
				func(classname, value gjson.Result) bool {
					if classname.String() == "flowRecord" {
						var child NetflowRecords
						child.Description = types.StringValue(value.Get("attributes.description").String())
						child.CollectParameters = types.StringValue(value.Get("attributes.collectParams").String())
						child.MatchParameters = types.StringValue(value.Get("attributes.matchParams").String())
						mapKey := value.Get("attributes.name").String()
						if data.Records == nil {
							data.Records = make(map[string]NetflowRecords)
						}
						data.Records[mapKey] = child
					}
					return true
				},
			)
			return true
		},
	)
	res.Get(data.getClassName() + ".children").ForEach(
		func(_, v gjson.Result) bool {
			v.ForEach(
				func(classname, value gjson.Result) bool {
					if classname.String() == "flowMonitor" {
						var child NetflowMonitors
						child.Description = types.StringValue(value.Get("attributes.description").String())
						mapKey := value.Get("attributes.name").String()
						value.Get("children").ForEach(
							func(_, nestedV gjson.Result) bool {
								nestedV.ForEach(
									func(nestedClassname, nestedValue gjson.Result) bool {
										if nestedClassname.String() == "flowExporterBucket" {
											var nestedChildflowExporterBucket NetflowMonitorsExporterBuckets
											nestedChildflowExporterBucket.Description = types.StringValue(nestedValue.Get("attributes.description").String())
											nestedChildflowExporterBucket.HashHigh = types.Int64Value(nestedValue.Get("attributes.hashHi").Int())
											nestedChildflowExporterBucket.HashLow = types.Int64Value(nestedValue.Get("attributes.hashLo").Int())
											nestedMapKey := nestedValue.Get("attributes.id").String()
											if child.ExporterBuckets == nil {
												child.ExporterBuckets = make(map[string]NetflowMonitorsExporterBuckets)
											}
											child.ExporterBuckets[nestedMapKey] = nestedChildflowExporterBucket
										}
										return true
									},
								)
								return true
							},
						)
						if data.Monitors == nil {
							data.Monitors = make(map[string]NetflowMonitors)
						}
						data.Monitors[mapKey] = child
					}
					return true
				},
			)
			return true
		},
	)
	res.Get(data.getClassName() + ".children").ForEach(
		func(_, v gjson.Result) bool {
			v.ForEach(
				func(classname, value gjson.Result) bool {
					if classname.String() == "flowHwProfile" {
						var child NetflowHardwareProfiles
						child.Description = types.StringValue(value.Get("attributes.description").String())
						child.BurstIntervalShift = types.Int64Value(value.Get("attributes.burstIntervalShift").Int())
						child.ExportInterval = types.Int64Value(value.Get("attributes.exportInterval").Int())
						child.IpPacketIdShift = types.Int64Value(value.Get("attributes.ipPacketIdShift").Int())
						child.Mtu = types.Int64Value(value.Get("attributes.mtu").Int())
						child.SourcePort = types.Int64Value(value.Get("attributes.sourcePort").Int())
						mapKey := value.Get("attributes.name").String()
						if data.HardwareProfiles == nil {
							data.HardwareProfiles = make(map[string]NetflowHardwareProfiles)
						}
						data.HardwareProfiles[mapKey] = child
					}
					return true
				},
			)
			return true
		},
	)
	res.Get(data.getClassName() + ".children").ForEach(
		func(_, v gjson.Result) bool {
			v.ForEach(
				func(classname, value gjson.Result) bool {
					if classname.String() == "flowClassMap" {
						var child NetflowClassMaps
						mapKey := value.Get("attributes.name").String()
						value.Get("children").ForEach(
							func(_, nestedV gjson.Result) bool {
								nestedV.ForEach(
									func(nestedClassname, nestedValue gjson.Result) bool {
										if nestedClassname.String() == "flowMatchAcl" {
											var nestedChildflowMatchAcl NetflowClassMapsMatchAcls
											nestedMapKey := nestedValue.Get("attributes.name").String()
											if child.MatchAcls == nil {
												child.MatchAcls = make(map[string]NetflowClassMapsMatchAcls)
											}
											child.MatchAcls[nestedMapKey] = nestedChildflowMatchAcl
										}
										return true
									},
								)
								return true
							},
						)
						if data.ClassMaps == nil {
							data.ClassMaps = make(map[string]NetflowClassMaps)
						}
						data.ClassMaps[mapKey] = child
					}
					return true
				},
			)
			return true
		},
	)
}

// End of section. //template:end fromBody

// Section below is generated&owned by "gen/generator.go". //template:begin updateFromBody

func (data *Netflow) updateFromBody(res gjson.Result) {
	for key, item := range data.Exporters {
		var rflowExporter gjson.Result
		res.Get(data.getClassName() + ".children").ForEach(
			func(_, v gjson.Result) bool {
				if v.Get("flowExporter.attributes.name").String() == key {
					rflowExporter = v
					return false
				}
				return true
			},
		)
		if !rflowExporter.Exists() {
			delete(data.Exporters, key)
			continue
		}
		if !item.Description.IsNull() {
			item.Description = types.StringValue(rflowExporter.Get("flowExporter.attributes.description").String())
		} else {
			item.Description = types.StringNull()
		}
		if !item.DestinationIp.IsNull() {
			item.DestinationIp = types.StringValue(rflowExporter.Get("flowExporter.attributes.destinationIp").String())
		} else {
			item.DestinationIp = types.StringNull()
		}
		if !item.DestinationPort.IsNull() {
			item.DestinationPort = types.Int64Value(rflowExporter.Get("flowExporter.attributes.destinationPort").Int())
		} else {
			item.DestinationPort = types.Int64Null()
		}
		if !item.Dscp.IsNull() {
			item.Dscp = types.Int64Value(rflowExporter.Get("flowExporter.attributes.dscp").Int())
		} else {
			item.Dscp = types.Int64Null()
		}
		if !item.SourceInterface.IsNull() {
			item.SourceInterface = types.StringValue(rflowExporter.Get("flowExporter.attributes.sourceInterface").String())
		} else {
			item.SourceInterface = types.StringNull()
		}
		if !item.Version.IsNull() {
			item.Version = types.StringValue(rflowExporter.Get("flowExporter.attributes.version").String())
		} else {
			item.Version = types.StringNull()
		}
		if !item.VrfName.IsNull() {
			item.VrfName = types.StringValue(rflowExporter.Get("flowExporter.attributes.vrfName").String())
		} else {
			item.VrfName = types.StringNull()
		}
		data.Exporters[key] = item
	}
	for key, item := range data.Records {
		var rflowRecord gjson.Result
		res.Get(data.getClassName() + ".children").ForEach(
			func(_, v gjson.Result) bool {
				if v.Get("flowRecord.attributes.name").String() == key {
					rflowRecord = v
					return false
				}
				return true
			},
		)
		if !rflowRecord.Exists() {
			delete(data.Records, key)
			continue
		}
		if !item.Description.IsNull() {
			item.Description = types.StringValue(rflowRecord.Get("flowRecord.attributes.description").String())
		} else {
			item.Description = types.StringNull()
		}
		if !item.CollectParameters.IsNull() {
			item.CollectParameters = types.StringValue(rflowRecord.Get("flowRecord.attributes.collectParams").String())
		} else {
			item.CollectParameters = types.StringNull()
		}
		if !item.MatchParameters.IsNull() {
			item.MatchParameters = types.StringValue(rflowRecord.Get("flowRecord.attributes.matchParams").String())
		} else {
			item.MatchParameters = types.StringNull()
		}
		data.Records[key] = item
	}
	for key, item := range data.Monitors {
		var rflowMonitor gjson.Result
		res.Get(data.getClassName() + ".children").ForEach(
			func(_, v gjson.Result) bool {
				if v.Get("flowMonitor.attributes.name").String() == key {
					rflowMonitor = v
					return false
				}
				return true
			},
		)
		if !rflowMonitor.Exists() {
			delete(data.Monitors, key)
			continue
		}
		if !item.Description.IsNull() {
			item.Description = types.StringValue(rflowMonitor.Get("flowMonitor.attributes.description").String())
		} else {
			item.Description = types.StringNull()
		}
		for nc := range item.ExporterBuckets {
			ncItem := item.ExporterBuckets[nc]
			var rflowExporterBucket gjson.Result
			rflowMonitor.Get("flowMonitor.children").ForEach(
				func(_, v gjson.Result) bool {
					if v.Get("flowExporterBucket.attributes.id").String() == nc {
						rflowExporterBucket = v
						return false
					}
					return true
				},
			)
			if !rflowExporterBucket.Exists() {
				delete(item.ExporterBuckets, nc)
				continue
			}
			if !ncItem.Description.IsNull() {
				ncItem.Description = types.StringValue(rflowExporterBucket.Get("flowExporterBucket.attributes.description").String())
			} else {
				ncItem.Description = types.StringNull()
			}
			if !ncItem.HashHigh.IsNull() {
				ncItem.HashHigh = types.Int64Value(rflowExporterBucket.Get("flowExporterBucket.attributes.hashHi").Int())
			} else {
				ncItem.HashHigh = types.Int64Null()
			}
			if !ncItem.HashLow.IsNull() {
				ncItem.HashLow = types.Int64Value(rflowExporterBucket.Get("flowExporterBucket.attributes.hashLo").Int())
			} else {
				ncItem.HashLow = types.Int64Null()
			}
			item.ExporterBuckets[nc] = ncItem
		}
		data.Monitors[key] = item
	}
	for key, item := range data.HardwareProfiles {
		var rflowHwProfile gjson.Result
		res.Get(data.getClassName() + ".children").ForEach(
			func(_, v gjson.Result) bool {
				if v.Get("flowHwProfile.attributes.name").String() == key {
					rflowHwProfile = v
					return false
				}
				return true
			},
		)
		if !rflowHwProfile.Exists() {
			delete(data.HardwareProfiles, key)
			continue
		}
		if !item.Description.IsNull() {
			item.Description = types.StringValue(rflowHwProfile.Get("flowHwProfile.attributes.description").String())
		} else {
			item.Description = types.StringNull()
		}
		if !item.BurstIntervalShift.IsNull() {
			item.BurstIntervalShift = types.Int64Value(rflowHwProfile.Get("flowHwProfile.attributes.burstIntervalShift").Int())
		} else {
			item.BurstIntervalShift = types.Int64Null()
		}
		if !item.ExportInterval.IsNull() {
			item.ExportInterval = types.Int64Value(rflowHwProfile.Get("flowHwProfile.attributes.exportInterval").Int())
		} else {
			item.ExportInterval = types.Int64Null()
		}
		if !item.IpPacketIdShift.IsNull() {
			item.IpPacketIdShift = types.Int64Value(rflowHwProfile.Get("flowHwProfile.attributes.ipPacketIdShift").Int())
		} else {
			item.IpPacketIdShift = types.Int64Null()
		}
		if !item.Mtu.IsNull() {
			item.Mtu = types.Int64Value(rflowHwProfile.Get("flowHwProfile.attributes.mtu").Int())
		} else {
			item.Mtu = types.Int64Null()
		}
		if !item.SourcePort.IsNull() {
			item.SourcePort = types.Int64Value(rflowHwProfile.Get("flowHwProfile.attributes.sourcePort").Int())
		} else {
			item.SourcePort = types.Int64Null()
		}
		data.HardwareProfiles[key] = item
	}
	for key, item := range data.ClassMaps {
		var rflowClassMap gjson.Result
		res.Get(data.getClassName() + ".children").ForEach(
			func(_, v gjson.Result) bool {
				if v.Get("flowClassMap.attributes.name").String() == key {
					rflowClassMap = v
					return false
				}
				return true
			},
		)
		if !rflowClassMap.Exists() {
			delete(data.ClassMaps, key)
			continue
		}
		for nc := range item.MatchAcls {
			ncItem := item.MatchAcls[nc]
			var rflowMatchAcl gjson.Result
			rflowClassMap.Get("flowClassMap.children").ForEach(
				func(_, v gjson.Result) bool {
					if v.Get("flowMatchAcl.attributes.name").String() == nc {
						rflowMatchAcl = v
						return false
					}
					return true
				},
			)
			if !rflowMatchAcl.Exists() {
				delete(item.MatchAcls, nc)
				continue
			}
			item.MatchAcls[nc] = ncItem
		}
		data.ClassMaps[key] = item
	}
}

// End of section. //template:end updateFromBody

// Section below is generated&owned by "gen/generator.go". //template:begin toDeleteBody

func (data Netflow) toDeleteBody() nxos.Body {
	body := ""
	if body == "" {
		body, _ = sjson.Set(body, data.getClassName()+".attributes", map[string]interface{}{})
	}
	childrenPath := data.getClassName() + ".children"
	for key, child := range data.Exporters {
		deleteBody := ""
		deleteBody, _ = sjson.Set(deleteBody, "flowExporter.attributes.rn", child.getRn(key))
		deleteBody, _ = sjson.Set(deleteBody, "flowExporter.attributes.status", "deleted")
		body, _ = sjson.SetRaw(body, childrenPath+".-1", deleteBody)
	}
	for key, child := range data.Records {
		deleteBody := ""
		deleteBody, _ = sjson.Set(deleteBody, "flowRecord.attributes.rn", child.getRn(key))
		deleteBody, _ = sjson.Set(deleteBody, "flowRecord.attributes.status", "deleted")
		body, _ = sjson.SetRaw(body, childrenPath+".-1", deleteBody)
	}
	for key, child := range data.Monitors {
		deleteBody := ""
		deleteBody, _ = sjson.Set(deleteBody, "flowMonitor.attributes.rn", child.getRn(key))
		deleteBody, _ = sjson.Set(deleteBody, "flowMonitor.attributes.status", "deleted")
		body, _ = sjson.SetRaw(body, childrenPath+".-1", deleteBody)
	}
	for key, child := range data.HardwareProfiles {
		deleteBody := ""
		deleteBody, _ = sjson.Set(deleteBody, "flowHwProfile.attributes.rn", child.getRn(key))
		deleteBody, _ = sjson.Set(deleteBody, "flowHwProfile.attributes.status", "deleted")
		body, _ = sjson.SetRaw(body, childrenPath+".-1", deleteBody)
	}
	for key, child := range data.ClassMaps {
		deleteBody := ""
		deleteBody, _ = sjson.Set(deleteBody, "flowClassMap.attributes.rn", child.getRn(key))
		deleteBody, _ = sjson.Set(deleteBody, "flowClassMap.attributes.status", "deleted")
		body, _ = sjson.SetRaw(body, childrenPath+".-1", deleteBody)
	}

	return nxos.Body{Str: body}
}

func (data Netflow) toBodyWithDeletes(ctx context.Context, state Netflow, config Netflow) nxos.Body {
	body := data.toBody(config)
	bodyPath := data.getClassName() + ".children"
	_ = bodyPath
	for stateKey := range state.Exporters {
		if _, found := data.Exporters[stateKey]; !found {
			stateChild := state.Exporters[stateKey]
			deleteBody := ""
			deleteBody, _ = sjson.Set(deleteBody, "flowExporter.attributes.rn", stateChild.getRn(stateKey))
			deleteBody, _ = sjson.Set(deleteBody, "flowExporter.attributes.status", "deleted")
			body.Str, _ = sjson.SetRaw(body.Str, bodyPath+".-1", deleteBody)
		}
	}
	for stateKey := range state.Records {
		if _, found := data.Records[stateKey]; !found {
			stateChild := state.Records[stateKey]
			deleteBody := ""
			deleteBody, _ = sjson.Set(deleteBody, "flowRecord.attributes.rn", stateChild.getRn(stateKey))
			deleteBody, _ = sjson.Set(deleteBody, "flowRecord.attributes.status", "deleted")
			body.Str, _ = sjson.SetRaw(body.Str, bodyPath+".-1", deleteBody)
		}
	}
	for stateKey := range state.Monitors {
		if _, found := data.Monitors[stateKey]; !found {
			stateChild := state.Monitors[stateKey]
			deleteBody := ""
			deleteBody, _ = sjson.Set(deleteBody, "flowMonitor.attributes.rn", stateChild.getRn(stateKey))
			deleteBody, _ = sjson.Set(deleteBody, "flowMonitor.attributes.status", "deleted")
			body.Str, _ = sjson.SetRaw(body.Str, bodyPath+".-1", deleteBody)
		}
	}
	for di := range state.Monitors {
		if _, found := data.Monitors[di]; !found {
			continue
		}
		stateItemdi := state.Monitors[di]
		planItemdi := data.Monitors[di]
		matchBodyPathdi := ""
		for mi, mv := range gjson.Get(body.Str, bodyPath).Array() {
			if mv.Get("flowMonitor.attributes.rn").String() == stateItemdi.getRn(di) {
				matchBodyPathdi = bodyPath + "." + strconv.Itoa(mi) + ".flowMonitor.children"
				break
			}
		}
		if matchBodyPathdi == "" {
			continue
		}
		for stateChildKey := range stateItemdi.ExporterBuckets {
			if _, found := planItemdi.ExporterBuckets[stateChildKey]; !found {
				stateChild := stateItemdi.ExporterBuckets[stateChildKey]
				deleteBody := ""
				deleteBody, _ = sjson.Set(deleteBody, "flowExporterBucket.attributes.rn", stateChild.getRn(stateChildKey))
				deleteBody, _ = sjson.Set(deleteBody, "flowExporterBucket.attributes.status", "deleted")
				body.Str, _ = sjson.SetRaw(body.Str, matchBodyPathdi+".-1", deleteBody)
			}
		}
	}
	for stateKey := range state.HardwareProfiles {
		if _, found := data.HardwareProfiles[stateKey]; !found {
			stateChild := state.HardwareProfiles[stateKey]
			deleteBody := ""
			deleteBody, _ = sjson.Set(deleteBody, "flowHwProfile.attributes.rn", stateChild.getRn(stateKey))
			deleteBody, _ = sjson.Set(deleteBody, "flowHwProfile.attributes.status", "deleted")
			body.Str, _ = sjson.SetRaw(body.Str, bodyPath+".-1", deleteBody)
		}
	}
	for stateKey := range state.ClassMaps {
		if _, found := data.ClassMaps[stateKey]; !found {
			stateChild := state.ClassMaps[stateKey]
			deleteBody := ""
			deleteBody, _ = sjson.Set(deleteBody, "flowClassMap.attributes.rn", stateChild.getRn(stateKey))
			deleteBody, _ = sjson.Set(deleteBody, "flowClassMap.attributes.status", "deleted")
			body.Str, _ = sjson.SetRaw(body.Str, bodyPath+".-1", deleteBody)
		}
	}
	for di := range state.ClassMaps {
		if _, found := data.ClassMaps[di]; !found {
			continue
		}
		stateItemdi := state.ClassMaps[di]
		planItemdi := data.ClassMaps[di]
		matchBodyPathdi := ""
		for mi, mv := range gjson.Get(body.Str, bodyPath).Array() {
			if mv.Get("flowClassMap.attributes.rn").String() == stateItemdi.getRn(di) {
				matchBodyPathdi = bodyPath + "." + strconv.Itoa(mi) + ".flowClassMap.children"
				break
			}
		}
		if matchBodyPathdi == "" {
			continue
		}
		for stateChildKey := range stateItemdi.MatchAcls {
			if _, found := planItemdi.MatchAcls[stateChildKey]; !found {
				stateChild := stateItemdi.MatchAcls[stateChildKey]
				deleteBody := ""
				deleteBody, _ = sjson.Set(deleteBody, "flowMatchAcl.attributes.rn", stateChild.getRn(stateChildKey))
				deleteBody, _ = sjson.Set(deleteBody, "flowMatchAcl.attributes.status", "deleted")
				body.Str, _ = sjson.SetRaw(body.Str, matchBodyPathdi+".-1", deleteBody)
			}
		}
	}
	return body
}

// End of section. //template:end toDeleteBody
