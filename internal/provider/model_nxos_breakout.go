// Copyright © 2023 Cisco Systems, Inc. and its affiliates.
// All rights reserved.
//
// Licensed under the Mozilla Public License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://mozilla.org/MPL/2.0/
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// SPDX-License-Identifier: MPL-2.0

// Code generated by "gen/generator.go"; DO NOT EDIT.

package provider

// Section below is generated&owned by "gen/generator.go". //template:begin imports

import (
	"context"
	"fmt"
	"strconv"

	"github.com/CiscoDevNet/terraform-provider-nxos/internal/provider/helpers"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/netascode/go-nxos"
	"github.com/tidwall/gjson"
	"github.com/tidwall/sjson"
)

// End of section. //template:end imports

// Section below is generated&owned by "gen/generator.go". //template:begin types

type Breakout struct {
	Device  types.String               `tfsdk:"device"`
	Dn      types.String               `tfsdk:"id"`
	Modules map[string]BreakoutModules `tfsdk:"modules"`
}

type BreakoutModules struct {
	FrontPanelPorts map[string]BreakoutModulesFrontPanelPorts `tfsdk:"front_panel_ports"`
}

type BreakoutModulesFrontPanelPorts struct {
	BreakoutMap types.String `tfsdk:"breakout_map"`
}

type BreakoutIdentity struct {
	Device types.String `tfsdk:"device"`
}

func (data *BreakoutIdentity) toIdentity(ctx context.Context, plan *Breakout) {
	if plan.Device.IsNull() {
		data.Device = types.StringValue("")
	} else {
		data.Device = plan.Device
	}
}

func (data *Breakout) fromIdentity(ctx context.Context, identity *BreakoutIdentity) {
	if identity.Device.ValueString() == "" {
		data.Device = types.StringNull()
	} else {
		data.Device = identity.Device
	}
}

// End of section. //template:end types

// Section below is generated&owned by "gen/generator.go". //template:begin getPath

func (data Breakout) getDn() string {
	return "sys/breakout"
}

func (data BreakoutModules) getRn(key string) string {
	return fmt.Sprintf("module-%d", helpers.Must(strconv.ParseInt(key, 10, 64)))
}

func (data BreakoutModulesFrontPanelPorts) getRn(key string) string {
	return fmt.Sprintf("fport-%d", helpers.Must(strconv.ParseInt(key, 10, 64)))
}

func (data Breakout) getClassName() string {
	return "imBreakout"
}

// End of section. //template:end getPath

// Section below is generated&owned by "gen/generator.go". //template:begin toBody

func (data Breakout) toBody(config Breakout) nxos.Body {
	body := ""
	body, _ = sjson.Set(body, data.getClassName()+".attributes", map[string]interface{}{})
	var attrs string
	childrenPath := data.getClassName() + ".children"
	for key, child := range data.Modules {
		attrs = "{}"
		attrs, _ = sjson.Set(attrs, "id", key)
		body, _ = sjson.SetRaw(body, childrenPath+".-1.imMod.attributes", attrs)
		{
			nestedIndex := len(gjson.Get(body, childrenPath).Array()) - 1
			nestedChildrenPath := childrenPath + "." + strconv.Itoa(nestedIndex) + ".imMod.children"
			for key, child := range child.FrontPanelPorts {
				attrs = "{}"
				attrs, _ = sjson.Set(attrs, "id", key)
				if !child.BreakoutMap.IsUnknown() && !child.BreakoutMap.IsNull() {
					attrs, _ = sjson.Set(attrs, "breakoutMap", child.BreakoutMap.ValueString())
				}
				body, _ = sjson.SetRaw(body, nestedChildrenPath+".-1.imFpP.attributes", attrs)
			}
		}
	}

	return nxos.Body{Str: body}
}

// End of section. //template:end toBody

// Section below is generated&owned by "gen/generator.go". //template:begin fromBody

func (data *Breakout) fromBody(res gjson.Result) {
	res.Get(data.getClassName() + ".children").ForEach(
		func(_, v gjson.Result) bool {
			v.ForEach(
				func(classname, value gjson.Result) bool {
					if classname.String() == "imMod" {
						var child BreakoutModules
						mapKey := value.Get("attributes.id").String()
						value.Get("children").ForEach(
							func(_, nestedV gjson.Result) bool {
								nestedV.ForEach(
									func(nestedClassname, nestedValue gjson.Result) bool {
										if nestedClassname.String() == "imFpP" {
											var nestedChildimFpP BreakoutModulesFrontPanelPorts
											nestedChildimFpP.BreakoutMap = types.StringValue(nestedValue.Get("attributes.breakoutMap").String())
											nestedMapKey := nestedValue.Get("attributes.id").String()
											if child.FrontPanelPorts == nil {
												child.FrontPanelPorts = make(map[string]BreakoutModulesFrontPanelPorts)
											}
											child.FrontPanelPorts[nestedMapKey] = nestedChildimFpP
										}
										return true
									},
								)
								return true
							},
						)
						if data.Modules == nil {
							data.Modules = make(map[string]BreakoutModules)
						}
						data.Modules[mapKey] = child
					}
					return true
				},
			)
			return true
		},
	)
}

// End of section. //template:end fromBody

// Section below is generated&owned by "gen/generator.go". //template:begin updateFromBody

func (data *Breakout) updateFromBody(res gjson.Result) {
	for key, item := range data.Modules {
		var rimMod gjson.Result
		res.Get(data.getClassName() + ".children").ForEach(
			func(_, v gjson.Result) bool {
				if v.Get("imMod.attributes.id").String() == key {
					rimMod = v
					return false
				}
				return true
			},
		)
		if !rimMod.Exists() {
			delete(data.Modules, key)
			continue
		}
		for nc := range item.FrontPanelPorts {
			ncItem := item.FrontPanelPorts[nc]
			var rimFpP gjson.Result
			rimMod.Get("imMod.children").ForEach(
				func(_, v gjson.Result) bool {
					if v.Get("imFpP.attributes.id").String() == nc {
						rimFpP = v
						return false
					}
					return true
				},
			)
			if !rimFpP.Exists() {
				delete(item.FrontPanelPorts, nc)
				continue
			}
			if !ncItem.BreakoutMap.IsNull() {
				ncItem.BreakoutMap = types.StringValue(rimFpP.Get("imFpP.attributes.breakoutMap").String())
			} else {
				ncItem.BreakoutMap = types.StringNull()
			}
			item.FrontPanelPorts[nc] = ncItem
		}
		data.Modules[key] = item
	}
}

// End of section. //template:end updateFromBody

// Section below is generated&owned by "gen/generator.go". //template:begin toDeleteBody

func (data Breakout) toDeleteBody() nxos.Body {
	body := ""
	if body == "" {
		body, _ = sjson.Set(body, data.getClassName()+".attributes", map[string]interface{}{})
	}
	childrenPath := data.getClassName() + ".children"
	for key, child := range data.Modules {
		deleteBody := ""
		deleteBody, _ = sjson.Set(deleteBody, "imMod.attributes.rn", child.getRn(key))
		deleteBody, _ = sjson.Set(deleteBody, "imMod.attributes.status", "deleted")
		body, _ = sjson.SetRaw(body, childrenPath+".-1", deleteBody)
	}

	return nxos.Body{Str: body}
}

func (data Breakout) toBodyWithDeletes(ctx context.Context, state Breakout, config Breakout) nxos.Body {
	body := data.toBody(config)
	bodyPath := data.getClassName() + ".children"
	_ = bodyPath
	for stateKey := range state.Modules {
		if _, found := data.Modules[stateKey]; !found {
			stateChild := state.Modules[stateKey]
			deleteBody := ""
			deleteBody, _ = sjson.Set(deleteBody, "imMod.attributes.rn", stateChild.getRn(stateKey))
			deleteBody, _ = sjson.Set(deleteBody, "imMod.attributes.status", "deleted")
			body.Str, _ = sjson.SetRaw(body.Str, bodyPath+".-1", deleteBody)
		}
	}
	for di := range state.Modules {
		if _, found := data.Modules[di]; !found {
			continue
		}
		stateItemdi := state.Modules[di]
		planItemdi := data.Modules[di]
		matchBodyPathdi := ""
		for mi, mv := range gjson.Get(body.Str, bodyPath).Array() {
			if mv.Get("imMod.attributes.rn").String() == stateItemdi.getRn(di) {
				matchBodyPathdi = bodyPath + "." + strconv.Itoa(mi) + ".imMod.children"
				break
			}
		}
		if matchBodyPathdi == "" {
			continue
		}
		for stateChildKey := range stateItemdi.FrontPanelPorts {
			if _, found := planItemdi.FrontPanelPorts[stateChildKey]; !found {
				stateChild := stateItemdi.FrontPanelPorts[stateChildKey]
				deleteBody := ""
				deleteBody, _ = sjson.Set(deleteBody, "imFpP.attributes.rn", stateChild.getRn(stateChildKey))
				deleteBody, _ = sjson.Set(deleteBody, "imFpP.attributes.status", "deleted")
				body.Str, _ = sjson.SetRaw(body.Str, matchBodyPathdi+".-1", deleteBody)
			}
		}
	}
	return body
}

// End of section. //template:end toDeleteBody
